[project]
name = "integration-lab"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.27",
    "psycopg[binary]>=3.3.5",
    "pydantic>=2.7",
    "uvicorn>=0.53.0",
]
